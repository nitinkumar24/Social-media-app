
<div class="ui threaded comments" id="reply_<%=reply.id%>">
   <div class="comments-content">
      <% user = reply.user %>

      <div class="comment">
         <a class="avatar">
            <%= image_tag user.avatar.url(:medium),:class =>"profile_image",:alt=>"" %>

         </a>
         <div class="content">
            <a class="author">
               <%= render 'profile/profile_link' ,user: user %>

            </a>
            <div class="metadata">
               <span class="date"><%= reply.created_at.localtime.strftime("%d %b %I:%M%p") %></span>
               <% if reply.user.id == current_user.id or reply.post.user.id == current_user.id or reply.comment.user.id == current_user.id %>

                   <%= link_to 'Delete', reply, method: :delete, remote:true, data: { confirm: 'Are you sure?' } %>
                   <% if reply.user.id == current_user.id %>
                       <%= link_to 'Edit', edit_reply_path(reply) %>
                   <% end %>
               <% end %>
            </div>


            <div class="text">
               <%= reply.content %>


            </div>




         </div>
      </div>


   </div>


</div>