<br>
<%= form_with(model: post, local: true) do |form| %>
  <% if post.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(post.errors.count, "error") %> prohibited this post from being saved:</h2>

      <ul>
      <% post.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.text_field :content, id: :post_content, id:"user_email" %>
  </div>


    <div class="anonymous" style="display: flex;float: right">
       <h3 >Anonymous
          <%= form.check_box :anonymous %>
       </h3>
       <div class="actions">
          <%= form.submit 'Update Post', :style => "border:2px solid black;padding:5px;float:right;border-radius:5px;margin-left:5px"   %>
       </div>
    </div>

<% end %>
