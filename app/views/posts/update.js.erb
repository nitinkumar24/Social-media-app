$('#post_form_<%= @post.id %>').find( "form" ).remove();
$('#post_orig_content_<%= @post.id %>').css('display','block').html('<%= @post.content %>');
noty({
    text: 'Post has been successfully updated',
    theme: 'metroui',
    type: 'alert',
    layout: 'topCenter',
    timeout: 1000

});
