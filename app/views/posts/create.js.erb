noty({
    text: 'Post was Created',
    theme: 'metroui',
    type: 'alert',
    layout: 'topCenter',
    timeout: 1000

});

$('#posts').prepend("<%= escape_javascript (render 'post', post: @post) %>");
$('#post_content').val("");

    $("#imgInp").val('');
    $(".dup-pic").css("display","none");

