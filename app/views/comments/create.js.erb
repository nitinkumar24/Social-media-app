$('#count_comments_<%=@comment.post.id%>').html(<%=@comment.post.comments.count%>);
$('#comments_<%=@comment.post.id%>').prepend("<%= escape_javascript (render 'comments/comment', comment: @comment) %>").addClass("ui threaded comments");
$('.comment-input').val("").css("height","30px");


console.log("hello comment");